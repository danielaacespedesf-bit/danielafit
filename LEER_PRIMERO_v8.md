# DanielaFit v8

- Esta versión integra fotos reales de referencia para cada ejercicio.
- Sube a GitHub los archivos de adentro de esta carpeta, no la carpeta completa.
- La carpeta `exercise-images/` debe quedar en la raíz del repo, junto a `index.html`.
- Nota: `cableRow` usa la misma foto de referencia que `seatedRow` en esta versión.
